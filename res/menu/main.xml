<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- Adds an item to the list. -->
    <item android:id="@+id/btn_open"
        android:title="@string/open"
        android:showAsAction="ifRoom|withText" />
</menu>
